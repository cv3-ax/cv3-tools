# 사용 가이드 (사원용)

자연어로 질문하면, 회사 DB에서 **읽기 전용**으로 조회해 **CSV 파일**로 뽑아주는 도구입니다.
SQL을 몰라도 됩니다. 데이터는 안전하게 **읽기만** 하며, 접속정보는 **내 PC에만** 저장됩니다.

## 1. 설치 (최초 1회 · 개발자 도움 권장)
PC에 **Claude Code** 와 **git** 이 설치돼 있어야 합니다.
1. 내 GitHub 계정에 이 PC의 **SSH 키 등록** (처음이면 개발자에게 요청)
2. Claude Code 에서 아래를 입력:
```
/plugin marketplace add git@github.com:cv3-ax/cv3-tools.git
/plugin install sql-builder@cv3-tools
```
> 개발자가 `deploy/onboard.ps1`(Windows) / `deploy/onboard.sh`(mac·linux) 스크립트로 한 번에 해줄 수도 있습니다.

## 2. 접속정보 등록 (최초 1회)
```
/sql-setup
```
물어보는 대로 입력하면 됩니다:

| 입력 | 설명 |
|------|------|
| host | DB 주소 (담당자 안내) |
| port | 보통 3306 |
| user / password | **본인 DB 계정** |
| database | 예: `labangba` |
| 스키마 파일 | 테이블 구조가 담긴 `.sql` 파일 경로 (담당자 제공) |

- 실제 접속을 시도해 **성공한 경우에만** 저장됩니다.
- 입력값은 **내 PC 로컬에만** 저장됩니다(어디로도 전송되지 않음).

## 3. 사용
```
/sql-query 지난달 카테고리별 매출 상위 20개
```
진행 방식:
1. 필요하면 몇 가지를 되물어봅니다 (기간·대상·지표 등)
2. **"이렇게 뽑을게요"** 를 사람 말로 보여주고 → **내가 확인한 뒤** 실행
3. 결과를 **다운로드 폴더** 에 `datalab_날짜시각.csv` 로 저장
4. 몇 행인지·파일 위치·미리보기를 알려줌

### 예시 질문
- `이번 달 라이브 방송별 매출 상위 30개 뽑아줘`
- `지난 분기 카테고리별 평균 판매가 알려줘`
- `특정 플랫폼 최근 방송 목록이랑 방문수`

## 4. 더 잘 쓰는 팁
- **기간·대상·원하는 값**(건수 / 매출 / 방문수 등)을 함께 말하면 정확해집니다.
- 결과가 아쉬우면 이어서 `기간을 6월로 바꿔서 다시` 처럼 다듬어 달라고 하면 됩니다.

## 5. 주의
- **읽기 전용**입니다 — 데이터 수정·삭제는 안 됩니다(요청해도 거절).
- 실행 직전 **확인 문구**를 꼭 읽고 진행하세요(엉뚱한 조건으로 뽑는 것 방지).

## 6. 잘 안 될 때
- 설치·SSH·계정 오류 → **개발자(담당자)에게 문의**
- `먼저 /sql-setup 을 실행하세요` 메시지 → **2번(접속정보 등록)** 부터 다시
- CSV가 안 보이면 → **다운로드 폴더** 확인
- 최신 기능이 필요하면 → `/plugin marketplace update cv3-tools`
